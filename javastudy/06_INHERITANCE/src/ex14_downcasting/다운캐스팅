다운캐스팅

1. downcasting
2. 부모 클래스 타입의 인스턴스를 자식 클래스 타입으로 변환하는 것이다.
3. 부모 클래스 타입으로 저장된 자식 클래스 인스턴스의 메소드 호출 문제(부모 타입으로 저장했기 때문에 부모 타입의 메소드만 보이는 것)를
	해결하는 방법 중 하나이다.
4. 강제로 변환해야 한다.
5. if문과 instanceof 연산자를 이용해서 안전하게 캐스팅하는 것이 좋다.