<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.mapper.staff">

	<!-- 1. 사원 목록 조회 -->
	<select id="selectStaffList" resultType="domain.StaffDTO">
		SELECT SNO, NAME, DEPT, SALARY
		  FROM STAFF
		 ORDER BY SNO ASC
	</select>

	<!-- 2. 사원 번호로 조회 -->
	<select id="selectStaffByNo" parameterType="String" resultType="domain.StaffDTO">
		SELECT SNO, NAME, DEPT, SALARY
		  FROM STAFF
		 WHERE SNO = #{query}
	</select>

	<!-- 3. 사원 등록 -->
	<insert id="insertStaff" parameterType="domain.StaffDTO">
		INSERT INTO STAFF
			(SNO, NAME, DEPT, SALARY)
		VALUES
			(#{sno}, #{name}, #{dept}, #{salary})
	</insert>
	

</mapper>